---
# ── Backend ──────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: altertrack-backend
  namespace: altertrack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: altertrack-backend
  template:
    metadata:
      labels:
        app: altertrack-backend
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: backend
          image: ghcr.io/axtazer/altertrack-backend:latest@sha256:df455f4c1ac0bb6b8e0fc47a228bd6c355f60316ff1e9d488aee00ef720ec9e3
          ports:
            - containerPort: 3001
          envFrom:
            - secretRef:
                name: altertrack-secrets
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3001"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: altertrack-backend
  namespace: altertrack
spec:
  selector:
    app: altertrack-backend
  ports:
    - port: 3001
      targetPort: 3001
---
# ── Frontend ─────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: altertrack-frontend
  namespace: altertrack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: altertrack-frontend
  template:
    metadata:
      labels:
        app: altertrack-frontend
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: frontend
          image: ghcr.io/axtazer/altertrack-frontend:latest@sha256:ad423ac051704c91518c0456ad2245ea07b8d63fef51bc31a7f05d58c7fd6c6b
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: altertrack-frontend
  namespace: altertrack
spec:
  selector:
    app: altertrack-frontend
  ports:
    - port: 80
      targetPort: 80
---
# ── Ingress ───────────────────────────────────────────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: altertrack-ingress
  namespace: altertrack
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  rules:
    - host: altertrack.castaldo.fr
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: altertrack-backend
                port:
                  number: 3001
          - path: /
            pathType: Prefix
            backend:
              service:
                name: altertrack-frontend
                port:
                  number: 80
