---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix
  namespace: matrix
spec:
  parentRefs:
    - name: shared-gateway
      namespace: kube-system
  hostnames:
    - matrix.axtazer.me
  rules:
    # Exact match, priorité sur le PathPrefix "/" ci-dessous (requis par Gateway API :
    # un match exact l'emporte toujours sur un prefix, quel que soit l'ordre déclaré,
    # mais on le met en premier pour la lisibilité)
    - matches:
        - path:
            type: Exact
            value: /.well-known/matrix/client
      backendRefs:
        - name: matrix-wellknown
          port: 8080
    - matches:
        - path:
            type: Exact
            value: /.well-known/matrix/server
      backendRefs:
        - name: matrix-wellknown
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: synapse
          port: 8008
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matrix-tunnel
  namespace: matrix
spec:
  ingressClassName: cloudflare-tunnel
  rules:
    - host: matrix.axtazer.me
      http:
        paths:
          # pathType Exact non supporté par ce controller (strrl.dev/
          # cloudflare-tunnel-ingress-controller) -- il rejette alors TOUT
          # l'Ingress (plus aucune route programmée, DNS cassé). Prefix sur ce
          # chemin précis reste sans risque : rien d'autre ne matche sous
          # /.well-known/matrix/client/*, et il précède le "/" plus générique.
          - path: /.well-known/matrix/client
            pathType: Prefix
            backend:
              service:
                name: matrix-wellknown
                port:
                  number: 8080
          - path: /.well-known/matrix/server
            pathType: Prefix
            backend:
              service:
                name: matrix-wellknown
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: synapse
                port:
                  number: 8008
